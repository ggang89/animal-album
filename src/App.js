
import Content from "./components/Content";
import TabBar from "./components/TabBar";

export default function App($app) {//index.js에서 전달한 요소를 받음
  
  const tabBar = new TabBar();
  const content = new Content();
}